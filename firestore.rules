rules_version = '2';
// The service talks to Firestore through the Admin SDK, which bypasses rules.
// No client access exists in this sandbox, so every client request is denied.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
